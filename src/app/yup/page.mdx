---
title: Yup validation example
description: Yup validation example
---

import YupFormExample from './YupFormExample';
import GithubFiles from '@/components/GithubFiles';
import Example from '@/components/Example';

# Yup validation example

Form input handling and [Yup](https://yup-docs.vercel.app/) validation with [vovk-yup](https://www.npmjs.com/package/vovk-yup). The request input is validated on the client-side before being sent to the server where it is validated again.

## Result

<Example>
  <YupFormExample />
</Example>

## Code

<GithubFiles paths={['src/modules/yup/UserYupController.ts', 'src/app/yup/YupFormExample.tsx']} />
