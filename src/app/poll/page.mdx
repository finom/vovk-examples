---
title: Polling example
description: Polling example
---

import PollExample from './PollExample';
import GithubFiles from '@/components/GithubFiles';
import Example from '@/components/Example';

# Polling example

Demonstrates infinite polling. The client sends `i` parameter to the server, the server increments it and streams the result back to the client. After 10 iterations, the server ends the response and the client starts polling again.

## Result

<Example>
  <PollExample />
</Example>

## Code

<GithubFiles paths={['src/modules/poll/PollController.ts', 'src/app/poll/PollExample.tsx']} />
