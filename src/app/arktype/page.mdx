---
title: Arktype validation example
description: Arktype validation example
---

import ArktypeFormExample from './ArktypeFormExample';
import GithubFiles from '@/components/GithubFiles';
import Example from '@/components/Example';

# Arktype validation example

Form input handling and [Arktype](https://https://arktype.io/) validation. The request input is validated on the client-side before being sent to the server where it is validated again.

## Result

<Example>
  <ArktypeFormExample />
</Example>

## Code

<GithubFiles paths={['src/modules/arktype/UserArktypeController.ts', 'src/app/arktype/ArktypeFormExample.tsx']} />
